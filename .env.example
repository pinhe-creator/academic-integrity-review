# 本地配置示例。把本文件复制为 .env 并填入你的值，本地运行时会自动加载。
# .env 已被 .gitignore 排除，不会提交到仓库。
#
# 想让“本地的访问/核查”也计入线上同一个中央计数器：
# 填入与线上同一套 Upstash 凭证即可（每次打开/搜索都会上报到该 Upstash）。
# 这两个值在 upstash.com 数据库页面的 “REST API” 区获取。
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=

# 可选：本地文件后端的计数文件路径（仅在未配置 Upstash 时生效），默认 ./stats.json
# STATS_FILE=./stats.json
